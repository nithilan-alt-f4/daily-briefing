services:
  - type: web
    name: daily-briefing
    runtime: image
    image:
      dockerfilePath: Dockerfile
    region: oregon
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
    autoDeploy: true
